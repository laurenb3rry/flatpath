# pyosmium publishes to PyPI under the name `osmium`. Version 4 is a hard
# requirement, not a preference: the parser uses the FileProcessor API, which
# earlier releases do not have.
osmium>=4.0
rasterio>=1.3
numpy>=1.24
pytest>=7.0
