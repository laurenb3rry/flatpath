# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride
._*

# --- Tooling / agent state ---
.claude/
.tokensave/
.serena/
.headroom*

PLAN.md
README.md

# --- Python (graphbuild) ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# raw OSM + DEM inputs — large, redownloadable
graphbuild/data/

# The built graph in FlatPath/Resources/ is committed: the app bundles it, so a
# clone has to be able to build without a 1.2 GB data download first.

# --- Swift / Xcode ---
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods / Carthage (unused today, cheap to keep)
Pods/
Carthage/Build/
